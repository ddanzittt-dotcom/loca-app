# LOCA

정적 웹앱 구조로 분리한 버전입니다.

## 폴더 구조

- `index.html`
- `css/style.css`
- `js/app.js`

## 실행 방법

브라우저에서 `index.html`을 열면 됩니다.

## GitHub Pages 배포

1. 이 폴더를 그대로 저장소 루트에 올립니다.
2. GitHub 저장소의 **Settings → Pages** 에서 배포 브랜치를 선택합니다.
3. 루트(`/`) 기준으로 배포하면 됩니다.
